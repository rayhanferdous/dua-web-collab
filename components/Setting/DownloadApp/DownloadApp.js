import Page from "../../Page";
import TopSection from "../Projects/TopSection";

const DownloadApp = () => {
  return <Page title={"Dowload Dua App"} subtitle={"Both android and iOS"} home={<TopSection/>} />;
};

export default DownloadApp;
